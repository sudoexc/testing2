// Функция для печати текста
function printText(text) {
    console.log(text);
    // Если вы хотите вывести текст на страницу, можно использовать что-то вроде:
    // document.body.innerHTML += text + '<br>';
}

// Текст, который нужно напечатать
const textToPrint = "Это текст, который будет напечатан через 3 секунды.";

// Устанавливаем таймер на 3 секунды
setTimeout(() => {
    printText(textToPrint);
}, 3000);

console.log("Скрипт запущен. Ожидайте 3 секунды...");
